# Run log — Dotfiles 10x roadmap overnight execution (2026-07-14/15)

Session: Opus 4.8 (1M) · ultracode · Codex adversarial-review gate (gpt-5.6-sol, high).
Baseline: `master @ 1f6b5dc`, clean, CI green, no open PRs.

Baseline metrics (measured 2026-07-14): repo ~232 MB (fonts 189 MB); tracked-sans-fonts 1.0 MB;
startup 240 ms median / 350 ms max; macOS CI ≈ 9 m 08 s, linux ≈ 67 s; Brewfile drift 99
unrecorded / 7 unmet; OMZ plugins on fresh bash-3.2 install: 0/4.

## Run summary (final — 2026-07-15)

**19 packets worked; ALL 19 MERGED; master ends green + clean @ `614e9ec`.**

> **Update 2026-07-15 (post-run, user-directed):** after the overnight run the user reviewed
> and cleared everything that had been parked/deferred: **P3-1** (nvim) completed as Option A
> (#118), the two parked drafts **P1-1** (#108 iTerm) and **P1-2** (#109 fonts) were rebased onto
> post-run master and merged, and **P2-2** (#119 layered dotbot configs) — unblocked once #108/#109
> landed — was completed. See each row for details. Remaining manual machine steps (iTerm + nvim
> migrations) are in HANDOFF.md.

| Disposition | Count | Packets |
|---|---|---|
| **Merged** (CI-green, adversarial-reviewed) | **19** | all P0-1..P0-5, P1-1..P1-4, P2-1..P2-8, P3-1, P3-2 |

Merged PRs: #100, #102–#119. Follow-up issue: #101 (P0-1 staging-swap). No commits to `master`
outside PRs except the end-of-run + post-run bookkeeping docs.

- **Startup:** 240 ms → 237 ms median (max 411 ms outlier), under the 300 ms budget.
- **CI:** both legs (linux ~1–2 m, macOS ~7 m) green on every merge; weekly cron + failure-notifier added (P2-5).
- **Gate calibration:** P0/P1 ran loop-to-approve-or-10; from P2-6 on, the tightened policy
  (≤medium+unreachable floor, 3-round cap on non-P0, exhaustiveness reserved for security/correctness).
  Every capped merge had all HIGH findings resolved; residuals were unreachable or ≤medium.
- **Harness lesson (mid-run):** the Codex gate uses `--scope working-tree`, so launch review on
  STAGED-uncommitted changes — reviewing after commit yields a vacuous empty-diff approve.

| Packet | Branch | PR# | Merge SHA | Review rounds | Result | Notes |
|---|---|---|---|---|---|---|
| P3-1 | feat/nvim-self-contained | #118 | fa7c96b | wf(5-lens) + 3 codex | **MERGED (Option A, user-directed post-run)** | Initially PARKED: investigation found the plan's premise was factually wrong — the live ~/.config/nvim is NOT a "2022 NvChad fossil" but a maintained **NvChad v2.5** lazy.nvim config (lazy-lock updated 2026-07-11), untracked, and the repo `nvim/custom` v1.0 overlay it was symlinked to is **never imported** (dead). Building the plan-as-written would have replaced the user's working editor with a config reconstructed from the dead overlay. Surfaced the fork (A adopt-live / B from-scratch / C drop); **user chose A**. Executed: git rm the dead nvim/custom; nvim/ = the live NvChad v2.5 config (27 pinned plugins); rewrote install_nvim.sh (no NvChad/Packer clone; Neovim 0.11+ version guard; per-plugin commit + clean-tree + submodule verification, **fail-closed**); both manifests link ~/.config/nvim→nvim as a whole-dir symlink in the FIRST link block (before install_nvim.sh) with an `if`-guard (Dotbot skips-not-fails while a real dir is present). Review = a 5-lens adversarial WORKFLOW (found+fixed: manifest link-ordering no-op, toothless boot check, fresh-Ubuntu-0.9.5 incompat, pre-migration ./install failure, runtime write-back churn) then 3 codex rounds (r1 2×high: per-plugin+fail-closed & version guard; r2 med: dirty-checkout verification; r3 high = **accepted residual**: init.lua's stable-branch lazy.nvim bootstrap runs before the lockfile pins it — the standard upstream NvChad/lazy pattern, hardening it would fork init.lua from upstream, contradicting adopt-as-is). Verified end-to-end: fresh-HOME dry-run 0 entries; headless install = 27/27 at locked commits, boot clean, exit 0; no-config + dirtied-plugin both fail closed exit 1; **macOS CI green (8m15s) = the real ./install ordering + fail-closed bootstrap works end-to-end**; Linux green (version-guard skip). **Machine migration = manual backup-first (HANDOFF).** |
| P2-7 | chore/claude-settings-hygiene | #117 | 8a0d19c | 3 → **approve** | **MERGED** | D5 config hygiene on claude/settings.json (shared cross-machine baseline). Removed: model (opus[1m]) + effortLevel (xhigh) pins; two DEAD unreferenced marketplaces (claude-code-plugins, villavicencio-skills-private — verified no enabledPlugin sources them); three host/network auto-allow rules (curl -fsSL*, curl -s*, ssh root@openclaw-prod*). Kept claude *, tmux -V*, all git/gh/brew/npm read rules, all hooks/statusLine/mcpServers, 6 enabledPlugins (all map to existing marketplaces — verified no orphans; dv + codex resolve). Added a // note key (Claude Code silently ignores unknown keys — verified). Review caught 2 REAL factual errors in my override guidance (both fixed): r1 — ~/.claude/settings.local.json is NOT a user-level Claude Code source (verified via claude-code-guide agent against official settings docs: 5 scopes, no user-level *.local; settings.local.json is project-scoped only) → note rewritten to reference only real layers; r2 — my 'until ./install re-links it' claim was wrong: live ~/.claude/settings.json is a REGULAR FILE and Dotbot links relink:true with NO force, so ./install won't clobber it → HANDOFF now documents the P2-7 change is INERT until a manual back-up→remove→relink→verify migration (do-not-auto-delete live user data). r3 clean approve. Both CI green. **Session-safe: live settings decoupled, so removal did NOT change this running session's permissions.** **Morning checklist: to actually apply P2-7 on the personal Mac, do the manual settings.json migration in HANDOFF.** |
| P3-2 | feat/agents-md | #116 | 0cddd88 | 3 → **approve** | **MERGED** | NEW AGENTS.md — tool-neutral canonical repo brief (layout, non-negotiable conventions, common 'adding things' tasks, verification-command table, branch/PR workflow, install pipeline, invariants). ACCEPTANCE VERIFIED: fresh `codex exec --sandbox read-only` in a temp dir with ONLY AGENTS.md answered 'add a brew package and verify' correctly, citing AGENTS.md. CLAUDE.md: non-destructive pointer (NO slim, NO @import — self-sufficiency guardrail + can't confirm the system-prompt injection expands @imports; **aggressive slim DEFERRED for an interactive follow-up**). Reconciled 2 stale CLAUDE.md facts caught by review: NVM shims are SIX (nvm node npm npx bb browse, verified vs _load_nvm), and `claude` installs via helpers/install_claude_code.sh native installer (curl install.sh → ~/.local/bin/claude), NOT a Homebrew cask (Brewfile has no claude entry). NEW bin/dot doctor 'docs cross-references' read-only check: strips fenced blocks, recognizes repo-relative paths DYNAMICALLY via os.listdir (covers .github/, dotbot/, ./install without a manual allowlist), captures mktemp+parser exit status (fail-closed), WARNs on unresolved refs. Review found real fail-open bugs both rounds: r1 fenced-block regex desync (examined ~1 of 28 refs); r2 incomplete prefix allowlist + ignored exit status + the 2 stale facts. r3 clean approve (real signal — reviewed staged diff). **Harness confirmed: launching review on STAGED-uncommitted changes gives real findings; committing-then-reviewing yields a vacuous empty-diff approve.** Both CI legs green. |
| P2-1 | feat/dot-cli | #115 | 1820515 | 3 (capped) | **MERGED** | New `bin/dot` bash dispatcher (no new deps), symlinked to `~/.local/bin/dot` via BOTH manifests (`~/.local/bin` added to create lists; already on PATH via zshenv). Subcommands: install/doctor/check/bench/drift/update/explain/docs-index. `doctor` is strictly read-only (verified zero-mutation, ~2s), exits non-zero on FAIL. Also quoted a pre-existing SC2046 in install_nvm.sh so `dot check` is green. All 3 review rounds raised legit fail-open/portability bugs, all fixed+tested: r1 — HANDOFF mtime crashed under gnubin-first PATH (BSD `stat -f` leaks `File:` into arithmetic under set -u) → Python os.path.getmtime; gitleaks scanner-error read as clean PASS → validate report + `git ls-files --error-unmatch` for tracked-only; symlink parser stderr/exit ignored → PARSER_ERROR+exit1 on degraded PyYAML. r2 — gitleaks report only syntax-validated (`{}` passes) → validate SHAPE (array of dicts) + capture extractor rc directly (heredoc, no proc-sub status loss); doctor didn't verify installed targets wired to repo → CONFLICTING_FILE/WRONG_TARGET via os.path.samefile (WARN, since app-managed regular files like ~/.claude/settings.json are legit — surfaced not fatal); missing/non-list config passed → MISSING_CONFIG+non-list PARSER_ERROR+require ≥1 link. Installed-target checks scoped to active-platform config (uname) so Linux window-meta.json doesn't false-positive on macOS. r3 review saw empty diff (committed before review — `--scope working-tree`; approve is vacuous) → merged at cap on own verification. **Surfaced real pre-existing drift: `~/.claude/settings.json` is a regular file, not the expected symlink → doctor WARNs (edits no longer write back to repo).** Both CI legs green. **Harness lesson: launch codex review on STAGED-uncommitted changes (round-1 pattern), not after commit, so `--scope working-tree` sees the diff.** |
| P2-2 | refactor/dotbot-layered-configs | #119 | 614e9ec | wf(5-lens) + 3 codex → **approve** | **MERGED (user-directed post-run, after #108/#109 landed)** | Split install.conf.yaml + install-linux.conf.yaml (both DELETED) into dotbot-conf/{base,darwin,linux}.yaml; `install` wrapper runs base then platform layer. **PROVEN dry-run equivalence**: base+darwin Would-create set byte-identical to old install.conf.yaml (35 dirs/symlinks), base+linux identical to old install-linux.conf.yaml (28), temp-HOME normalized. Only intentional change: dropped the contradictory chsh completion message. Linux locale kept in base.yaml (uname-guarded, no-op on macOS) since it must precede install_packages which base runs; dead non-Darwin locale block gone. chsh guarded on SHELL!=zsh. Helper cleanups: DOTFILES_DRY_RUN guards on install_nvm/brew/from_brewfile (+ dropped duplicate `. nvm.sh`), install_tmux log→$XDG_CACHE_HOME/dotfiles. bin/dot doctor+check parse all 3 configs (base+active-platform full, inactive sources-only); all old-manifest refs updated across docs+CI. Review = 5-lens workflow (4 clean) + 3 codex rounds; BOTH wrapper fixes were real: r1 best-effort regression (set -e skipped the platform layer on a benign base failure — restored the pre-split best-effort semantics), r2 fail-fast flag spellings (-x/--exit-on-failure exact-match missed clustered -xv/-nx + --exit* abbreviations while --except must NOT match), r3 approve. **macOS CI 8m56s = the full layered install runs base→darwin end-to-end** + all outcome assertions; Linux 53s. Machine-safe (repo-only; no migration). |
| P2-5 | ci/weekly-schedule | #114 | 8f95e16 | 2 | **MERGED** | install-matrix.yml: added weekly `schedule: cron '17 14 * * 1'` (Mon 6:17am PST; off top-of-hour per GitHub high-load-drop guidance — review r1 catch) + a `notify-on-schedule-failure` job (needs linux+macos; if failure()&&schedule) that opens/updates a title-deduped tracking issue. LEAST-PRIVILEGE: workflow stays contents:read (code-executing matrix jobs unchanged); only the notifier job elevates to issues:write. Verified: workflow_dispatch run green, PR matrix green, notify correctly skips on non-scheduled runs. approve r2. |
| P2-8 | fix/tmux-polish | #113 | 5b47d66 | 4 | **MERGED** | tmux.conf/general/display now source+reference via $HOME/.config/tmux (shell-independent — $DOTFILES shell-only, XDG can be unset for a service-started server; Dotbot always installs ~/.config; verified loads under `env -i HOME PATH`); symlinked general+display.conf in BOTH install manifests. status-interval 1→2 (spinner still cycles; not 5 — spinner has no refresh-client). Removed C-l identity bind + tmux<2.2 UTF-8 lines. window-meta.linux.json → {} (stale OpenClaw seed). Save-on-attach hook: iterated save.sh(race)→delete(lost fallback)→continuum_save.sh (interval+lock coordinator, race-safe, reviewer's own middle-path rec). Review r1-r2 caught REAL reachable bugs (Linux manifest omission, residual XDG dep, -b save-race) all fixed; final r4 finding = unreachable multi-server ownership edge already shared by the pre-existing status-right inline → merged substantively-satisfied (past cap). Both CI green. |
| P2-4 | chore/legacy-alias-curation | #112 | 72daf3a | 2 | **MERGED** | alias.sh: fixed lsd dir-filter; deleted ip/ips (kept localip), lscleanup, sniff/httpdump, emptytrash, spotoff/spoton, badge, map, chromekill, afk, obsolete `tmux -f` alias. functions.sh: deleted json/escape/unidecode/codepoint (missing pygmentize/perl-oneoffs); kept digga/tre; unset loop var. gitconfig: deleted dead `push=push -u`, [gitflow] blocks, [diff exif] textconv; gitattributes exif lines removed. Review r1 caught a real regression: bare `gh` credential helper is PATH-dependent (breaks GUI/IDE clients w/ minimal PATH) → KEPT absolute /opt/homebrew/bin/gh (both machines ARM; git config has no $BREW_PREFIX expansion) — a P0-4-style plan-step reversal. Every surviving alias resolves; image git-diff no longer errors. approve r2; both CI green. |
| P2-3 | fix/zsh-dead-config-sweep | #111 | 448bf6f | 3 (+2 CI-fix rounds) | **MERGED** | zshenv dead-config sweep (subprocess-free DOTFILES, removed NVM_LAZY/COMPLETION/MYVIMRC/LUA_PATH/HOMEBREW_INSTALL_CLEANUP, fixed 32³ comment); zshrc: replaced DEAD lazy fzf block with CAPABILITY-AWARE init (fzf --zsh >=0.48 else distro fallback — caught by review r1: Ubuntu 0.44 predates --zsh), hardcoded completion cache-path, plugin reorder (syntax-hl before history-substring per README), dropped zsh-256color (plugins+install_omz+R5), sleep 1, dropped dup NVM_DIR; iterm2 alias gate. ^T=fzf/^R=atuin/\ec=fzf verified via pty; startup 195ms<=240ms. **CI caught 2 real regressions my subprocess-free DOTFILES introduced**: (1) `${${:-..}:A:h:h}` is a FATAL 'bad substitution' when zshenv is `.`-sourced by dash during ./install → guarded on $ZSH_VERSION; (2) readlink POSIX-fallback gave wrong DOTFILES (repo PARENT) when ZDOTDIR unset (install_tmux.sh's bash context) → cascaded to R6 tmux-plugin failure → switched fallback to git derivation (ZDOTDIR-independent, like master). Lesson: zshenv is `.`-sourced by sh/bash in the install path; keep it POSIX-safe. Review approved r2+r3; both CI legs green. |
| P2-6 | docs/truth-pass | #110 | c047784 | 3 | **MERGED** | First packet under the tightened calibration (3-round cap + severity/reachability floor). Honest README (killed yourusername, CI badge, submodule-init dry-run caveat) + MIT LICENSE + CLAUDE.md truth fixes (removed non-existent osx/, fixed claude/ subsection + commands-in-plugin note, tmux-window-namer→dv plugin, corrected dry-run claim). Frontmatter: 7 module + 6 problem_type added, severity Title-cased, path-as-category fixed, backtick/colon YAML fixed → all 43 parse under real PyYAML. Flipped 5 stale plan statuses. OpenClaw-bearer addendum CORRECTED (container destroyed/host repurposed, risk-accepted not nil). New helpers/generate_docs_index.sh → INDEX.md: group-by-module/severity, vendored-PyYAML fail-closed validation, deterministic. All 3 review findings were REACHABLE+legit (calibration working: real bugs, not theoretical). Both CI legs green. |
| P1-2 | feat/fonts-via-casks | #109 | 4b412e1 | 9 + rebase | **MERGED (user-directed post-run)** | Un-parked + merged after the user confirmed glyphs render from the casks. Rebased onto post-run master (conflicts: install.conf.yaml fonts-lines removed alongside the run's dot/nvim/iterm links; CLAUDE.md + tmux.display.conf fonts truth-pass reapplied over P2-6/P2-8 edits; clean takes for Brewfile/install_packages/starship). Reconcile review caught ONE real gap — AGENTS.md (added by P3-2 AFTER this branch) still listed the deleted install_fonts.sh in its helper list → fixed to match CLAUDE.md; full-tree sweep confirmed no other live fonts-helper refs; dot doctor docs-cross-ref passes. Both CI legs green (macOS installed the casks + ran migrate_legacy_fonts). No machine step pending — casks already installed + glyphs confirmed. Original 9-round substance below. — Fonts→casks (font-fira-code-nerd-font + font-jetbrains-mono-nerd-font @3.4.0); git rm fonts/(96 files, D2 tip-only) + install_fonts.sh; install.conf.yaml font lines removed; starship swift glyph U+FBE3→U+E755 (verified absent/present via fonttools); doc truth pass (CLAUDE.md, tmux.display.conf). NEW helpers/migrate_legacy_fonts.sh (+legacy-font-hashes.txt): transactional (ERR/EXIT rollback trap) migration of the 16 colliding JetBrains fonts, runs from install_packages.sh before brew bundle; moves collisions to reversible backup outside repo, positive-ownership cask install, full rollback on failure; data-safe (user fonts preserved), bash-3.2/LC_ALL=C safe. Gate: real HIGH cask-collision bug fixed r1-2; r3-9 asymptotic-tail mediums all fixed+tested (43 assertions/14 scenarios). PARKED (machine-affecting, no approve). Residual open finding: concurrent-actor cask race — UNREACHABLE (sequential one-time install + brew's own lock). **Machine state: force-installed both casks on personal Mac during verification (brew install --cask --force); old manual Nerd Font files now orphaned in ~/Library/Fonts (harmless; cleanup optional).** Also found+fixed a real `$c…` non-UTF-8-locale parse bug. |
| P1-1 | feat/iterm-dynamic-profile | #108 | 4df9003 | 11 + rebase | **MERGED (user-directed post-run)** | Un-parked + merged after the user reviewed. Rebased onto post-run master (conflicts: install.conf.yaml iTerm link placed alongside the run's dot+nvim links; action.yml R3 PII-scan swapped in while preserving master R4-R7 incl. the R5 zsh-256color fix). Reconcile review → approve (R3/R4-R7 verified, iterm artifacts blob-identical to original). First CI hit a **flaky pre-apply assertion** (dry-run $HOME-delta caught macOS TV.app materializing `~/Movies/TV/*.strings` in an unpruned subtree — NOT my change; the iterm path is under the already-pruned $HOME/Library) → fixed by pruning $HOME/Movies,Music,Pictures from the snapshot. Both CI legs green after. Original 11-round substance below. — Replace live-synced iTerm prefs plist (custom-folder mode leaked usernames/host/paths) with PII-free Dynamic Profile + app-keymap sidecar + generator + README; delete plist(3702ln)+profile.json; widen CI R3 PII grep; new manual macOS `restore-iterm-app-prefs.sh --migrate` (disables LoadPrefsFromCustomFolder, backs up OUTSIDE repo via openat+O_NOFOLLOW+F_GETPATH+atomic+plist-validated copy). Gate ran 11 rounds, no `approve` (asymptotic tail on the backup helper); **all HIGH findings resolved by r9** (restore-path r3-4, migration-completeness r5, backup-outside-repo r6, XDG boundary+case-insensitive-FS bypass r7-8, symlink/parent-swap TOCTOU r9); r10-11 medium data-integrity (existing+fresh plist validation, atomic publish) all fixed+tested via extracted-Python failure-injection suites. PARKED not self-merged: deletes tracked files + rewrites live iTerm config → wants human review. Needs the manual `--migrate` step per machine. |
| P1-3 | chore/brewfile-curation | #107 | 9475d38 | 13 | **MERGED** | Curated Brewfile 103→89 (17 pure-dep attic + node/rbenv/ruby/python@3.10 dropped; 13 on-request libs retained flagged; ack/ag/screenfetch prune-flagged). REMOVED destructive export_deps. New read-only report_drift.sh (complete installed_on_request set, scope-aware config-independent npm dir read, all inventories validated). Review caught export_deps + drift bugs. |
| P1-4 | chore/delete-tracked-debris | #106 | 183bdd6 | 1 | **MERGED** | git rm 2 empty logs + 6 *.md.deprecated (superseded by dv skills); logs/ gitignored; drop ~/.claude/commands create. approve r1. |
| P0-5 | fix/remove-orphan-rigellute-tap | #105 | 79dfe67 | 1 | **MERGED** | Removed orphan tap "rigellute/tap" (nothing installs from it; bundle check identical). approve r1. |
| P0-4 | ci/drop-brew-bundle-jobs-pin | #104 | 0bcc938 | 5 | **MERGED (REVERSED)** | Adversarial gate caught a PLAN ERROR: deleting the serial brew-bundle pin would reintroduce flaky CI. Verified: run 29301199411 raced on Homebrew ≥6.0.5 (>the 5.1.12 "fix"). Pin KEPT; corrected the misleading "droppable" guidance in workflow comment + solution doc + HANDOFF CI-01 + plan P0-4. Also fixed false "real machines serial by default" claim (Homebrew 6.x = parallel auto). |
| P0-3 | fix/tmux-attention-pkill-anchor | #103 | 8537b51 | 2 | **MERGED** | Anchor spinner marker with -end; pkill via ERE matching new+legacy forms, non-digit boundary so %2 can't hit %20; manual sweep still works. Upgrade-safety (kill legacy loops) added round 2 → approve. |
| P0-2 | ci/outcome-assertions | #102 | 7ba8de3 | 8 | **MERGED** | Post-apply now asserts outcomes: R4 stderr-empty, R5 4 OMZ plugins, R6 TPM+4 tmux plugins (explicit list), R7 10x startup median<800ms w/ per-sample returncode+stderr+timeout. R6 de-escalated from config-parser to R5-style explicit list after 7 parser-edge rounds → approve. Both legs green (macos 7m49s validates P0-1's 4/4). |
| P0-1 | fix/omz-install-bash32 | #100 | 73eec36 | 11 | **MERGED** | bash-3.2 name\|url array, set -euo pipefail, pinned installer 677a459, forced ZSH=OMZ_DIR, checkout_complete completeness check, quarantine-not-delete, collision-safe paths, symmetric rollback, EXIT/INT/TERM trap. Gate hit round cap on 1 residual (uncatchable-signal crash-atomicity; non-data-losing, self-heals) → **user chose Merge now** (gate substantively satisfied; keystone unblocks P0-2). Concurrency finding rebutted+accepted. Follow-up: staging-swap → issue #101. |

## Gate calibration change (mid-run, user decision)
After P1-2 round 9, the user flagged gpt-5.6-sol as over-adversarial (asymptotic tail:
real bugs in rounds 1-2, theoretical concurrency edges in rounds 3-9). **New policy for
remaining packets (P2-6 onward): keep gpt-5.6-sol, but merge/park as soon as surviving
findings are ≤medium AND not reachable in the real execution context; hard-cap non-P0
packets at 3 review rounds; reserve full exhaustiveness for security/correctness-critical
code.** P0/P1 packets already done under the old (loop-to-approve-or-10) policy.

## Machine-state changes made during the run (personal Mac)
- **Font casks force-installed** (P1-2 verification): `brew install --cask --force
  font-jetbrains-mono-nerd-font font-fira-code-nerd-font` — both now installed (3.4.0).
  Old manually-installed Nerd Font files remain orphaned in `~/Library/Fonts` (harmless;
  `migrate_legacy_fonts.sh` handles the collision cleanup on a fresh `./install`). If P1-2
  (#109) is NOT merged, the Brewfile on master still lacks these casks — a `brew bundle
  cleanup` would flag them as unlisted. No action needed unless you run cleanup before merge.

## Morning-checklist additions discovered during the run
- **P2-8 reboot-restore verify (once #113 merges + `./install` relinks tmux):** the
  save-on-attach resurrect hook is now guarded on `session_windows > 1`, so a fresh/empty
  server can't clobber a good snapshot. After a real reboot: confirm continuum auto-restores
  your multi-window session (it should), and that attaching to it re-saves (windows>1). If a
  session ever fails to restore, check `~/.config/tmux/resurrect/last` still points at a
  multi-window save. P2-8 also newly symlinks `tmux.general.conf`+`tmux.display.conf` into
  `~/.config/tmux/` — `./install` (or `dotbot`) must run once to create them, else tmux.conf's
  new `$XDG_CONFIG_HOME/tmux/*.conf` sources won't resolve.
- **P1-1 is PARKED as draft PR #108** (not merged). It deletes tracked iTerm files and
  changes live iTerm config, so it wants a human review before merge. On merge, run the
  one-time manual migration on each Mac (iTerm2 **quit**): `helpers/restore-iterm-app-prefs.sh --migrate`
  — it disables `LoadPrefsFromCustomFolder`, backs up the old prefs outside the repo, then
  relaunch iTerm2 → Preferences → Profiles → select **Dotfiles** → mark default. Until merged +
  migrated, iTerm will keep rewriting the (now git-ignored) `iterm/com.googlecode.iterm2.plist`
  on disk; that's expected and no longer reaches git.
- **`~/.config/zsh/ohmyzsh`** — stray OMZ clone (git HEAD 677a459) created 2026-07-14 19:11 by
  the P0-1 acceptance test's ZDOTDIR leak (installer computes `$ZDOTDIR/ohmyzsh` when ZDOTDIR is
  set ≠ HOME). Inert — the real OMZ is `~/.oh-my-zsh` (Jun 5) and the shell pins `ZSH=~/.oh-my-zsh`.
  Safe to `rm -rf ~/.config/zsh/ohmyzsh`. NOT deleted by the agent (data-safety rule).
